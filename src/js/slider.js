window.addEventListener('load', ()=>{
	let slides = document.querySelectorAll('#slider #slider__fade img');
	const size = slides.length;
	let inicio = 0;

	showimage(0);
})

const showimage = (number) =>{
	
}

function back(){

}
